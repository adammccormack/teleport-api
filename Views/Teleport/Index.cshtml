@using DemoLibrary2.Models
@model dynamic


@{
    ViewData["Title"] = "Index";
}

<h2>Teleport Scores</h2>


<div class="mb-3">
    <label class="form-label">Select Continent</label>
    <select class="form-select" asp-items="Model.ContinentsSelectList"></select>
</div>


@*<table>

        @foreach (UrbanAreasModel.NorthAmericaUrbanAreas.UaItem city in Model.NorthAmericaCities)
        {
            <tr>
                <td>@city.name</td>
                <td>@city.href</td>
            </tr>
        }
    </table>

*@

@*<table>

        @foreach (ScoresModel.NorthAmericaScores.Root score in Model.NorthAmericaScore)
        {
            <tr>
                <td>@score.teleport_city_score.</td>
                <td>@score.summary</td>
            </tr>
        }
    </table>*@


